version: '3.8'
services:
  fastapi:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8007 --reload
    volumes:
      - .:/app
    ports:
      - "8007:8007"
    environment:
      - PYTHONUNBUFFERED=1
